import { SynonymsFilterPipe } from './synonyms-filter.pipe';

describe('SynonymsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SynonymsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
