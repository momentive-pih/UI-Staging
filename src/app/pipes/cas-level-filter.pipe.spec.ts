import { CasLevelFilterPipe } from './cas-level-filter.pipe';

describe('CasLevelFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CasLevelFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
