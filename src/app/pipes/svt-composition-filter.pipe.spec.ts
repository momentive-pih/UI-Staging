import { SvtCompositionFilterPipe } from './svt-composition-filter.pipe';

describe('SvtCompositionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SvtCompositionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
