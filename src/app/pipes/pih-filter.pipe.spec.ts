import { PihFilterPipe } from './pih-filter.pipe';

describe('PihFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PihFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
