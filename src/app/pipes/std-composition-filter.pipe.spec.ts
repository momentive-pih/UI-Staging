import { StdCompositionFilterPipe } from './std-composition-filter.pipe';

describe('StdCompositionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StdCompositionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
