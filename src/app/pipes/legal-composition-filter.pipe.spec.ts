import { LegalCompositionFilterPipe } from './legal-composition-filter.pipe';

describe('LegalCompositionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LegalCompositionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
