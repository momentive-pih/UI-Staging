import { ProductLevelPipe } from './product-level.pipe';

describe('ProductLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
