import { GhsLabelingPipe } from './ghs-labeling.pipe';

describe('GhsLabelingPipe', () => {
  it('create an instance', () => {
    const pipe = new GhsLabelingPipe();
    expect(pipe).toBeTruthy();
  });
});
